import UnavailablePanel from "@/app/components/UnavailablePanel";

export default function EmailPage() {
  return (
    <UnavailablePanel
      title="Email"
      description="Email inbox and reply workflows are not connected for the pilot. No email messages are read or sent by LegacyOS."
    />
  );
}
