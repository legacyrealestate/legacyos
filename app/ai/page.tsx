import UnavailablePanel from "@/app/components/UnavailablePanel";

export default function AiPage() {
  return (
    <UnavailablePanel
      title="AI Tools"
      description="AI recommendation tools are not enabled for the supervised pilot. Operational records can only be changed through deterministic staff-approved workflows."
    />
  );
}
