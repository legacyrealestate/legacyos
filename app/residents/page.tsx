import UnavailablePanel from "@/app/components/UnavailablePanel";

export default function ResidentsPage() {
  return (
    <UnavailablePanel
      title="Residents"
      description="Resident ledger, lease, and password reset workflows are not connected. Use maintenance intake for supported resident maintenance requests."
    />
  );
}
