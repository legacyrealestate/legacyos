import UnavailablePanel from "@/app/components/UnavailablePanel";

export default function CommandPage() {
  return (
    <UnavailablePanel
      title="Command Center"
      description="Command Center AI is not enabled for the supervised pilot. Staff actions must be performed through the ticket, vendor, document, and activity workflows."
    />
  );
}
