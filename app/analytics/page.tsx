import UnavailablePanel from "@/app/components/UnavailablePanel";

export default function AnalyticsPage() {
  return (
    <UnavailablePanel
      title="Analytics"
      description="Analytics dashboards are not connected for the pilot. The dashboard shows only live ticket, vendor, notification, and activity counts from Supabase."
    />
  );
}
