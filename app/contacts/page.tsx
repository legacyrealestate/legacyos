import UnavailablePanel from "@/app/components/UnavailablePanel";

export default function ContactsPage() {
  return (
    <UnavailablePanel
      title="Contacts"
      description="Contact sync is not connected for the pilot. Use the Vendors page for approved maintenance vendor records."
    />
  );
}
