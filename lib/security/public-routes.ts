export const PUBLIC_API_ROUTES = new Set([
  "/api/elevenlabs",
  "/api/twilio/status",
]);

export const PUBLIC_PAGE_ROUTES = new Set([
  "/auth/callback",
  "/login",
  "/privacy-policy",
  "/terms-and-conditions",
]);
